# Agent instructions

See [CLAUDE.md](CLAUDE.md).

<!-- A pointer rather than a symlink: Git for Windows defaults to
     core.symlinks=false, so a symlink checks out as a text file containing the
     literal target path. A pointer rather than a copy, so the two cannot
     drift. -->
